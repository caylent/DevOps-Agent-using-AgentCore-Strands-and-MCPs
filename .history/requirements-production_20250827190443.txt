# Production Dependencies - AWS DevOps Agent
# Clean requirements for production use only

# Core Strands SDK (requires access)
strands>=0.1.0

# MCP Protocol support
mcp>=0.1.0

# AWS SDK (optional - MCP servers handle AWS API calls)
boto3>=1.26.0